# ─── RovoDev Serve ────────────────────────────────────────────────────────────
# Port where `rovodev serve` is running (default: 8000)
# Start RovoDev with: rovodev serve 8000
ROVODEV_PORT=8000

# ─── AI Gateway (fallback when RovoDev is not running) ────────────────────────
# AI_GATEWAY_URL=
# ASAP_ISSUER=
# ASAP_KID=
# ASAP_PRIVATE_KEY=

# ─── Server ──────────────────────────────────────────────────────────────────
# PORT=8080
# DEBUG=false
